{% extends 'EventBundle::layout.html.twig' %}

{% block body %}
    {# this is rude but I want to be respected #}
    Hello you damn motherfucking {{ name|upper }}!<br/>
    {% for i in 1..count %}
        count is <strong>{{ i }}</strong><br/>
    {% endfor %}
    <h4>Event found</h4>
    name: {{ event.name }} <br/>
    location: {{ event.location }} <br/>
{% endblock %}